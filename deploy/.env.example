# Copy to .env next to deploy.sh and adjust.

# Image tag to deploy (matches the GitHub Release / git tag without the "v").
VERSION=latest

# PostgreSQL password used by both the database and mc-tool.
POSTGRES_PASSWORD=change-me
